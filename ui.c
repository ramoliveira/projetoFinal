/* Interface de Usuário(User Interface - UI)*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "ui.h"

/* Objetivo: Desenhar o cabeçalho com uma mensagem dentro.
Entrada: 
Saída: */

void cabecalho(char *texto) {
	int i;
	CSE
	for (i = 1; i <= (strlen(texto) + 2); i++) {
		HOR
	}
	CSD
	VEE
	printf(" %s ", texto); 
	VED
	CIE
	for (i = 1; i <= (strlen(texto) + 2); i++) {
		HOR
	}
	CID
} 
